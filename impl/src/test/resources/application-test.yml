spring:
  datasource:
    url: ${DB_URL}
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  flyway:
    enabled: off

  jpa:
    hibernate:
      ddl-auto: none

cloudinary-url: cloudinary://enter your cloudinary_url
cloudinary-download-folder: test/